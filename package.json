{
  "dependencies": {
    "autoprefixer": "^10.4.13",
    "postcss": "^8.4.19"
  },
  "scripts": {
    "build-css": "tailwindcss build -i style.css -o css/style.css",
    "tailwind": "tailwindcss -i style.css -o css/style.css --watch",
    "start": "browser-sync start --server --startPath /index.html --files '*.html,*.css,*.js'"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.2.4"
  }
}
